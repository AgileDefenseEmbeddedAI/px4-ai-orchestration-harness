# Agile Labs UI Kit

Recreates a product page from the Agile Labs sub-brand — the "Mission
Impact" vertical. Interactive sidebar swaps the detail panel between
problem areas.

Components: `LabsHeader`, `StatsBanner`, `StatGrid`, `FixSection`,
`ProblemSidebar`, `ProblemDetail`. Data in `PROBLEMS` array. Open `index.html`.
